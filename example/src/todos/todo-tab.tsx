import Flat, { FlatComponent } from "../../../dist";

export class TodoTab extends FlatComponent {
  render() {
    return (
      <div>
        <button>all</button>
        <button>done</button>
      </div>
    );
  }
}
